{
  "name": "github-stats-cards",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted GitHub stats SVG cards, generated by CI and served from GitHub Pages.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node src/index.js",
    "demo": "node src/index.js --demo"
  }
}
